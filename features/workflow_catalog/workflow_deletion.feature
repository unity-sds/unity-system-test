Feature: The workflow catalog shall allow deletion of workflows. Workflows can be deleted from the system;
  but references to the workflow should persist for provenance purposes- this essentially acts as a deprecation
  or 'do not user' flag